{
  "name": "clinic-saas-deploy",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "rm -rf app && mkdir app && unzip -q clinic-saas-source.zip -d app && cd app && npm install && npm run build && cd .. && node inject-drug-intelligence.cjs"
  }
}
